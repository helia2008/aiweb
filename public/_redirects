# Cloudflare Pages 重定向规则
# 1) 接住 /sitemap.xml 默认探测，指向 @astrojs/sitemap 实际生成的索引
/sitemap.xml /sitemap-index.xml 301

# 2) 全局去尾斜杠：canonical / sitemap 均为无斜杠形式，
#    避免 Cloudflare 对目录式 URL 返回 200 造成重复地址。
/:splat/ /:splat 301
