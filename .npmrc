# npm 项目级配置
# registry 用国内镜像，加速下载（团队共享，无盘符问题）
registry=https://registry.npmmirror.com/

# 缓存放项目内，避免占用 C 盘（当前 C 盘仅剩数百 MB）
# 相对路径，随仓库走，盘符无关
cache=.npm-cache

# 命中本地缓存优先：重装依赖时跳过网络往返（360 实时扫描环境下能省几分钟）
prefer-offline=true

# npm 与构建脚本都按「不带审计、不带赞助信息」跑，减少无谓网络请求
audit=false
fund=false
